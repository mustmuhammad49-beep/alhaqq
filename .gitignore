# Local Netlify folder
.netlify

# Environment variables — never commit
.env
.env.*
.env.local

# Claude Code local settings (may contain API keys)
.claude/settings.local.json

# Node dependencies
node_modules/

# Impeccable design-hook local cache/config
.impeccable/

# Scratch/temp files — never deploy
.extract-*.txt
deploy-entry44.bat
wheel-hub-prototype.html
